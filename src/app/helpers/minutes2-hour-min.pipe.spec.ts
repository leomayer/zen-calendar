import { Minutes2HourMinPipe } from './minutes2-hour-min.pipe';

describe('Minutes2HourMinPipe', () => {
  it('create an instance', () => {
    const pipe = new Minutes2HourMinPipe();
    expect(pipe).toBeTruthy();
  });
});
